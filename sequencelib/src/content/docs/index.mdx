---
title: Sequencelib
description: A platform for formalizing sequences from The On-Line Encyclopedia of Integer Sequences (OEIS).
template: splash
next: false
---

import Search from '../../components/FrontSearch.astro';
import RandomSequence from '../../components/RandomSequence.astro';
import { Card } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';
import { LinkCard } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { result } from "../../components/lean_info";

#### A platform for formalizing sequences from The On-Line Encyclopedia of Integer Sequences (OEIS)

<div class="relative">
  This site contains the formalization in <a href="https://lean-lang.org">Lean 4</a> of a collection of sequences from
  <a href="https://oeis.org">OEIS</a>. It includes metaprogramming tools that index the sequences, display values
  (example), and display equivalences among them (example).
  <a href="/getting_started/about">
    <span class="badge absolute -right-0 -bottom-0 badge-outline badge-primary">
      Read more
      <Icon name="right-arrow" class="mt-0" />
    </span>
  </a>
</div>

<div class="sl-flex justify-center mt-12 text-2xl">
  Explore Sequencelib
</div>
<div class="sl-flex justify-center">
  <Badge class="mx-2" text={`${result.num_seqs.toLocaleString()} sequences`} variant="success" size="medium" />
  <Badge class="mx-2" text={`${result.num_thms.toLocaleString()} theorems`} variant="tip" size="medium" />
</div>
<div class="sl-flex justify-center mt-4">
  <Search />
</div>
<div class="sl-flex justify-center mt-0">
  <RandomSequence />
  <LinkButton href="/A000">
    Browse Index
  </LinkButton>
</div>

